# Baseline local M-02 — 2026-08-27

Este baseline é um retrato do checkout local em `HEAD`
`154efcd94c97d77d7061ac557e6d6cbf17472395`. Ele não afirma equivalência com
`origin/develop`, CI, Neon ou produção.

## Estado e inventário

| Medição                                                                              | Resultado local                                         |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| Branch                                                                               | `program/v5-fechamento-sdd`                             |
| Worktree                                                                             | `dirty`; alterações preservadas, sem limpeza automática |
| BFF modules                                                                          | 8                                                       |
| `createServerFn` declarations                                                        | 30                                                      |
| Operações concretas/aliases                                                          | 31                                                      |
| Rotas API                                                                            | 4                                                       |
| Ocorrências lexicais antes da extração (`context.transaction`/`request.transaction`) | 42                                                      |
| Ocorrências atuais após a extração (fallbacks/allowlist)                             | 13                                                      |
| Arquivos alcançáveis com DB/import de persistência (matriz atual)                    | 21                                                      |
| Queries batch de dashboard                                                           | 6, invariável no teste de performance                   |
| Queries batch de products                                                            | 5, invariável no teste de performance                   |

As contagens de BFF, operações, sites transacionais e arquivos são geradas por
`npm run m02:matrix:generate`; a matriz compilada é `matrix.yaml`. `transactionSites`
e `directDatabaseFiles` são dimensões distintas: o primeiro conta ocorrências
transacionais classificadas; o segundo conta arquivos de persistência no inventário.

## Checks executados

- `npm run typecheck` — passou.
- `npm run lint -- --quiet` — passou.
- `npm test -- --reporter=dot` — 26 arquivos e 276 testes passaram.
- `npm run m02:matrix:check` — passou sem drift.
- `npm run m02:boundaries` — passou; alcançabilidade de DB em BFF está
  limitada à allowlist ou a repositories.
- `npm run m02:state:check` — deve passar enquanto o marker do ledger refletir
  o `HEAD` observado; este check é somente leitura.
- `npm run check:ui-stack`, `npm run check:no-supabase-runtime`, `npm run build`
  e `npm run check:bundle` — passaram no checkout local.

## Limites

Não foram medidos neste baseline: p50/p95 real, saturação de pool, cold
activation, Web Vitals, `pg_stat_statements`, E2E de ambiente publicado,
credenciais reais, Neon persistente, CI ou cutover. Esses itens permanecem em
M-06/M-07 e nos Qs externos/humanos correspondentes.
