# M-04 — Decisões de draft

As decisões abaixo são `AGENT-PROPOSED`. Elas organizam o draft a partir do RAT
SA-12/Bayes, mas não substituem a decisão humana de Q-019.

| ID        | Pergunta                                                        | Proposta para o draft                                                                                                                                                                                                         | Evidência / impacto                                                                                                                                                                                   | Estado           |
| --------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| M04-D-001 | `BudgetService`/`BudgetRepositoryPort` formais ou facade única? | Formalizar as duas portas; manter `budget-ledger.server.ts` como facade fina de compatibilidade                                                                                                                               | Alinha M-04 ao padrão de portas de M-02; reduz regra de negócio em facade                                                                                                                             | `AGENT-PROPOSED` |
| M04-D-002 | Como contar retry, round e `usageId`?                           | Um `usageId` por round; todas as tentativas internas compartilham o identificador; novo round ganha novo identificador                                                                                                        | Comportamento observado em `chat.functions.ts` e `chat-execution.server.ts`; evita multiplicação contábil                                                                                             | `AGENT-PROPOSED` |
| M04-D-003 | 429/backoff consome ou devolve reserva?                         | Default conservador: não criar nova reserva, manter a admissão da chamada em `model_call_count`, contar falha separadamente, liquidar tokens reais disponíveis e liberar somente a reserva de tokens não consumidos           | Hoje 429 é `RATE_LIMIT` sem retry automático; alternativas “reembolso total” e “consumo da reserva estimada” ficam abertas para Q-019                                                                 | `AGENT-PROPOSED` |
| M04-D-004 | Como fechar Q-010?                                              | `recordOutcome` durável antes do settlement; settlement muda status/counters; sweep com outcome reconcilia e sweep sem outcome expira                                                                                         | `ai_usage` já tem `outcome`/`real_tokens`; não exige tabela nova para a primeira correção                                                                                                             | `AGENT-PROPOSED` |
| M04-D-005 | Como implementar eventos?                                       | Outbox tenant-scoped, append transacional, chave estável de idempotência, entrega at-least-once e consumidores idempotentes                                                                                                   | Parte de `event.contracts.ts`; exactly-once não é prometido                                                                                                                                           | `AGENT-PROPOSED` |
| M04-D-006 | Quais falhas precisam de célula própria?                        | Usar a matriz F-01..F-20 em `failure-matrix.md`, incluindo crash entre outcome/settle, retry de settlement, sweep, RLS, outbox e E6                                                                                           | Torna settlement verificável por desfecho em vez de uma única célula de request                                                                                                                       | `AGENT-PROPOSED` |
| M04-D-007 | Qual invariante de TTL?                                         | `reservationTtlMs >= 120000 ms`, pois o timeout máximo de request é 60000 ms; testar o limite inferior e o valor válido                                                                                                       | Já há default e validação no ledger; a spec transforma configuração em invariante                                                                                                                     | `AGENT-PROPOSED` |
| M04-D-008 | Como impedir a corrida entre `settle` e `expire`?               | Usar claims atômicos tenant-scoped, condicionados ao estado ainda reservado; `expire` só pode vencer com `status = 'reserved'` e `outcome IS NULL`; o perdedor relê o estado e retorna no-op idempotente                      | A ordem precisa ser definida junto com D-004: se `recordOutcome` precede `settle`, um predicado literal `outcome IS NULL` não pode ser usado pelo `settle`; Q-019 deve ratificar a máquina de estados | `AGENT-PROPOSED` |
| M04-D-009 | O que acontece em `settle-after-expire`?                        | Uma expiração já confirmada não pode ser sobrescrita por um resultado tardio; o settlement retorna no-op, e o sweep não pode transformar outcome conhecido em `ttl_expired`                                                   | Fecha a célula de corrida tardia e evita dupla liberação de `tokens_reserved`/`in_flight`                                                                                                             | `AGENT-PROPOSED` |
| M04-D-010 | Como tornar E6 determinístico sem alterar runtime?              | O harness usa um gate lógico após as oito tentativas de reserva, mantém os dois admitidos bloqueados até as seis rejeições estarem observáveis e aceita `E6_HOLD_MS`/`E6_QUEUE_MS` somente como parâmetros bounded de fixture | A prova de concorrência deixa de depender de wall-clock; o clock injetável continua reservado aos testes temporais do ledger                                                                          | `AGENT-PROPOSED` |

## Regras de congelamento

- Nenhuma decisão acima muda para `FROZEN` nesta etapa.
- Q-019 deverá registrar a alternativa escolhida para D-003 e confirmar os campos
  finais do contrato de outbox.
- Q-019 deverá resolver a compatibilidade entre D-004 e D-008: `recordOutcome`
  pode ocorrer antes do settlement, mas um `settle` posterior não pode exigir
  simultaneamente `outcome IS NULL`. A implementação não deve esconder essa
  escolha em um predicate implícito.
- D-009 define o resultado terminal de um settlement tardio; não autoriza
  sobrescrever uma expiração já aplicada nem rebaixar um outcome conhecido.
- D-010 é uma decisão de estabilidade do harness. O gate é somente de teste e
  não altera `executeSendChatMessage`, o ledger de produção, schema ou migrations.
- A implementação não pode interpretar `AGENT-PROPOSED` como autorização para
  migration ou mudança de comportamento antes do congelamento.
- Divergências de RAT bloqueiam o congelamento, mas não invalidam o draft.

---

## Emenda v2 — 2026-08-28 (SDD v5.2 §8.1)

| ID        | Pergunta                                         | Proposta para o draft                                                                                                                                                                                                                                 | Evidência / impacto                                                                                                                                         | Estado                                                     |
| --------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| M04-D-011 | Como resolver a incompatibilidade D-004 × D-008? | Máquina de estados com CAS por linha (`RESERVED`→`COMPLETED`→`SETTLED`; `RESERVED`→`EXPIRED`): `recordOutcome` transita para `COMPLETED`; `settle` exige `status='COMPLETED'`; `expire` exige `status='RESERVED'` e TTL vencido pelo relógio do banco | Elimina o predicado literal `outcome IS NULL`; EvalPlanQual garante exatamente um gravador por transição; chegadas tardias com D-009 estrito (F-21/F-22/G7) | `AGENT-PROPOSED` (canônico do SDD v5.2; pendente de Q-019) |

Regra adicional de congelamento: Q-019 deverá ratificar D-011 como resolução de
D-004 × D-008, junto com D-003 e os campos finais do contrato de outbox; G1–G8
passam a compor o DoD de P9.

---

## Emenda v3 — 2026-09-13 (Onda 1, §14.3)

| ID        | Pergunta                                           | Proposta para o draft                                                                                                                                                                                                                                                                                                                                               | Evidência / impacto                                                                                                                                                                                                                                                                                                                                                                                                       | Estado           |
| --------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| M04-D-012 | Como vincular as execuções de tool ao round de IA? | O vínculo canônico é **N:1 via `tool_executions.usage_id`**: um round tem um `usage_id` (D-002) e pode executar N tools, cada uma com sua linha em `tool_executions` e seu `tool_call_id`; `ai_usage.tool_execution_id` permanece reservada e **não** é preenchida com "last tool wins", pois isso sobrescreveria o vínculo das execuções anteriores do mesmo round | Migration 0012 (`tool_call_id text`, `input jsonb`, `usage_id uuid` + índice `(tenant_id, usage_id)`, sem FK — `ai_usage` não tem `UNIQUE(tenant, id)`); threading de `toolCallId`/`usageId` em `runRegisteredTool` ← `runToolCall` ← `executeReservedRound` (`reservationResult.usageId`); replay idempotente preserva o vínculo; testes `src/test/tool-runner.persistence.test.ts` e `scripts/db/test-tool-security.ts` | `AGENT-PROPOSED` |

Regra adicional de congelamento: D-012 é decisão de contrato de dados e não
autoriza escrita em `ai_usage.tool_execution_id` nem migration adicional. Q-019
deverá ratificá-la junto de D-011.
