# Runbook — Homologação hPanel (11 itens, executar ANTES de apontar o domínio)

> Pré-requisito: BLOCKER-EXT-01 desbloqueado (plano Hostinger ativo + sessão assistida). Usar a **URL preview temporária** (`https://<preview-host>`), nunca o domínio canônico. Nenhum item sai de `BLOCKED` sem valor observado. Evidência em `docs/evidence/hpanel-homologacao-<AAAA-MM-DD>/` (captura, log ou saída; **nunca valores de secrets** — só presente/ausente).

| #   | Item                 | Comando exato                                                                                                        | Saída esperada                                                                                                                                                                                      | Evidência                                         | FAIL / ABORT                                                      |
| --- | -------------------- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------- |
| 1   | Node ≥ 24.15         | `node --version && npm --version`                                                                                    | `v24.15.0`+                                                                                                                                                                                         | `01-node-version.{log,png}`                       | `FAIL` + **ABORT geral** se teto < 24.15.0                        |
| 2   | Start customizado    | start = `npm run start` (`node .output/server/index.mjs`); `ps aux \| grep -F '.output/server/index.mjs'`            | processo do artefato publicado                                                                                                                                                                      | `02-start-cmd.{log,png}` (SHA + ps)               | `FAIL` se sem comando custom ou processo divergente               |
| 3   | Injeção de porta     | `env \| grep -E '^(PORT\|NITRO_PORT)=' \| sed 's/=.*/=<presente>/'` + log de boot                                    | `PORT` ou `NITRO_PORT` chega ao processo                                                                                                                                                            | `03-port.{log,png}` (só presença)                 | `FAIL` se nada chega ou porta efetiva diverge                     |
| 4   | Host bind            | `env \| grep -E '^(HOST\|NITRO_HOST)=' \| sed 's/=.*/=<presente>/'`                                                  | bind compatível com o proxy; preview responde                                                                                                                                                       | `04-host.{log,png}`                               | `FAIL` se preview não roteia                                      |
| 5   | Persistência         | 3× `curl -s -o /dev/null -w '%{http_code}\n' https://<preview-host>/api/health/live` em 15 min sem tráfego           | `200` nas 3                                                                                                                                                                                         | `05-persistencia.log` (timestamps)                | `FAIL` se dorme (reavaliar plano)                                 |
| 6   | Restart              | `kill <pid>`; 60 s; `ps` + `curl .../live`                                                                           | novo PID; `live → 200`                                                                                                                                                                              | `06-restart.log` (PIDs + timestamps)              | `FAIL` se não ressuscita                                          |
| 7   | Logs sem secrets     | abrir stdout/stderr; `grep -Ei 'postgres://\|api[_-]?key\|secret\|password\|token' <trecho> \| wc -l`                | consultáveis, retenção declarada; grep = `0`                                                                                                                                                        | `07-logs.{log,png}` (redigido)                    | `FAIL` se inacessíveis ou **qualquer segredo em claro**           |
| 8   | Proxy timeout > 60 s | `time curl -s -o /dev/null -w '%{http_code} %{time_total}\n' -X POST https://<preview-host>/api/chat --max-time 120` | sem corte ≤ 60 s                                                                                                                                                                                    | `08-proxy-timeout.log` (teto medido)              | `FAIL` se corte ≤ 60 s (ajustar `AI_REQUEST_TIMEOUT_MS` ou plano) |
| 9   | Egress TLS           | 3 sondas HTTPS públicas (Neon, Resend, gateway IA), sem credenciais                                                  | os 3 alcançáveis                                                                                                                                                                                    | `09-egress.log`                                   | `FAIL` por serviço; **ABORT** se Neon bloqueado                   |
| 10  | Health live+ready    | `curl -i https://<preview-host>/api/health/live` e `.../ready`                                                       | `live = 200`; `ready = 200` (ou `503` = app viva + banco indisponível: aceito aqui, bloqueia A4)                                                                                                    | `10-health-*.log`                                 | `FAIL` se `live ≠ 200`                                            |
| 11  | Secrets por ambiente | conferir NOMES no painel; `env \| grep -c DATABASE_ADMIN_URL` no web = `0`                                           | `DATABASE_URL`, `DATABASE_DRIVER=node-postgres`, `BETTER_AUTH_*`, `AUTH_TRUSTED_ORIGINS`, `RESEND_*`, `AI_GATEWAY_*` presentes; `DATABASE_ADMIN_URL`/`SUPABASE_*`/`MIGRATION_*` **ausentes** do web | `11-secrets.{png,log}` (nomes + limites da conta) | `FAIL` se ADMIN no web ou secrets entre ambientes                 |

**Gate de saída:** 11/11 `PASS` com evidência → atualizar `docs/evidence/hostinger-hpanel-verification.md`; só então apontar o domínio e iniciar A4 (`a4-a5-cutover.md`).
